#ifndef META_HPP
#define META_HPP

/*
  Aggregates and labels function pointers to mutable members of engine classes.
 */

namespace Turbine {
// typedef std::function<> Getter;

// struct MetaDB {
//     std::vector<>
// };
}

#endif
